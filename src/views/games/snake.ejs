<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Snake Game</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Your existing styles here */
    </style>
</head>
<body class="gradient-bg min-h-screen flex items-center justify-center p-4">
    <div class="bg-white bg-opacity-90 p-8 rounded-xl shadow-2xl text-center max-w-md w-full">
        <h1 class="text-4xl font-bold mb-6 text-gray-800 tracking-tight">🐍 Snake Game</h1>
        
        <!-- User and game session info -->
        <div class="mb-4 text-sm text-gray-600">
            Player: <%= user.username %> 
            <br>
            Session ID: <%= gameId %>
        </div>

        <div class="relative">
            <canvas id="gameCanvas" width="400" height="400" class="bg-gray-50"></canvas>
            
            <div id="overlay" class="absolute inset-0 flex items-center justify-center bg-black bg-opacity-50 rounded-lg">
                <div class="text-white text-center p-4">
                    <h2 class="text-2xl font-bold mb-4">Press Start to Play!</h2>
                    <p class="text-sm mb-2">Use arrow keys to control the snake</p>
                </div>
            </div>
        </div>

        <div class="mt-6 space-y-4">
            <div class="flex justify-center space-x-4">
                <button id="startButton" class="game-button px-6 py-3 bg-emerald-500 text-white rounded-lg font-semibold hover:bg-emerald-600">
                    Start Game
                </button>
                <button id="restartButton" class="game-button hidden px-6 py-3 bg-blue-500 text-white rounded-lg font-semibold hover:bg-blue-600">
                    Play Again
                </button>
            </div>
            
            <div class="flex justify-between items-center px-4">
                <div id="score" class="score-display text-2xl font-bold text-gray-800">Score: 0</div>
                <div id="highScore" class="score-display text-2xl font-bold text-gray-800">High Score: 0</div>
            </div>
        </div>
    </div>

    <!-- Add gameId to window object for access in snake.js -->
    <script>
        window.GAME_ID = '<%= gameId %>';
    </script>
    <script src="snake.js"></script>
</body>
</html>