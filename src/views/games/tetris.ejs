<!DOCTYPE html>
<html>
<head>
    <title>Tetris</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body class="bg-gradient-to-br from-purple-900 via-gray-900 to-black min-h-screen flex items-center justify-center p-4">
    <div class="bg-gray-800 rounded-xl shadow-2xl p-8 max-w-4xl w-full">
        <div class="flex flex-col md:flex-row items-center justify-between gap-8">
            <!-- Secțiunea de informații -->
            <div class="text-center md:text-left space-y-6 md:w-1/3">
                <div>
                    <h1 class="text-5xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-pink-600 mb-2">
                        TETRIS
                    </h1>
                    <h3 class="text-gray-400">Session Id: <%=gameId%></h3>
                </div>

                <div class="bg-gray-700 rounded-lg p-4 shadow-inner">
                    <h2 class="text-2xl font-bold text-white mb-2">Score</h2>
                    <p class="text-4xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-green-400 to-blue-500">
                        <span id="score">0</span>
                    </p>
                </div>

                <div class="space-y-4">
                    <button id="startBtn" class="w-full bg-gradient-to-r from-purple-500 to-pink-500 hover:from-purple-600 hover:to-pink-600 text-white font-bold py-3 px-6 rounded-lg transform transition hover:scale-105">
                        Play
                    </button>
                    
                    <a id="tryAgainBtn" href="/home/game/tetris" class="hidden w-full bg-gradient-to-r from-green-500 to-blue-500 hover:from-green-600 hover:to-blue-600 text-white font-bold py-3 px-6 rounded-lg transform transition hover:scale-105 block text-center">
                        Try again
                    </a>

                    <div class="bg-gray-700 rounded-lg p-4">
                        <h3 class="text-white font-bold mb-2">Controls:</h3>
                        <ul class="text-gray-300 text-sm space-y-1">
                            <li>↑ Rotate</li>
                            <li>← → Move</li>
                            <li>↓ Fast Down</li>
                        </ul>
                    </div>
                </div>
            </div>

            <!-- Containerul pentru joc -->
            <div class="relative">
                <div class="absolute inset-0 bg-gradient-to-r from-purple-500/20 to-pink-500/20 rounded-lg filter blur-xl"></div>
                <canvas id="tetris" class="relative rounded-lg border-4 border-gray-700 shadow-2xl"></canvas>
            </div>
        </div>
    </div>
    <script>window.GAME_ID = '<%=gameId%>'</script>
    <script src="/tetris.js"></script>
</body>
</html>