<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="/styles.css" rel="stylesheet">
</head>
<body class="bg-gray-100 flex items-center justify-center h-screen">
    <div class="bg-white shadow-md rounded-lg p-6 max-w-sm w-full">
        <h2 class="text-center text-xl font-bold mb-2"><%= user.username %></h2>
        <p class="text-center text-gray-600 mb-2">Email: <%= user.email %></p>
        <p class="text-center text-gray-500 mb-4">Money: <%= user.money %></p>

        <!-- Search Form -->
        <form action="/home/profile/search" method="POST" class="mt-4">
            <input type="text" name="username" placeholder="Search for a profile with username..." 
                   class="border border-gray-300 rounded-lg p-2 w-full mb-2" required>
            <button type="submit" 
                    class="bg-blue-500 text-white rounded-lg px-4 py-2 w-full hover:bg-blue-600">
                Search
            </button>
        </form>

        <!-- Back to Homepage Button -->
        <div class="mt-4">
            <a href="/home"
               class="inline-block px-6 py-3 bg-blue-500 hover:bg-blue-600 text-white font-semibold rounded-lg transition-colors duration-200 w-full text-center">
               Back to Homepage
            </a>
        </div>
    </div>
</body>
</html>
